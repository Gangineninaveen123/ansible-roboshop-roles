MYSQL_ROOT_PASSWORD: "{{ lookup('amazon.aws.aws_ssm', '/roboshop/mysql/mysql_root_passwword', region='us-east-1', decrypt=True ) }}" # because its already encrypted , so we are decripting make its True , and also it is safe to push in github, because it don't have password and nothing...., password was already set in the  SSM PARAMETER STORE